# Metal 0.33 Patch Set

These are crates.io source copies used to force the current Metal dependency
graph onto `metal = "=0.33.0"`.

Only the vendored `Cargo.toml` files should differ from the published crate
sources. Remove these patches when upstream `statumen` and `signinum` releases
publish the same Metal dependency bump.
