//! GPU-gated route cache and session helpers.
//!
//! Metal and CUDA-specific implementation currently lives alongside the route
//! orchestration in `export`; new GPU helpers should be added here instead of
//! growing the export module.
